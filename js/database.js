const DB='sentieriDB',STORE='routes';export function openDB(){return new Promise((ok,no)=>{const r=indexedDB.open(DB,1);r.onupgradeneeded=e=>{const d=e.target.result;if(!d.objectStoreNames.contains(STORE))d.createObjectStore(STORE,{keyPath:'id',autoIncrement:true})};r.onsuccess=()=>ok(r.result);r.onerror=()=>no(r.error)})}function req(r){return new Promise((ok,no)=>{r.onsuccess=()=>ok(r.result);r.onerror=()=>no(r.error)})}export const getAll=db=>req(db.transaction(STORE).objectStore(STORE).getAll());export const add=(db,x)=>req(db.transaction(STORE,'readwrite').objectStore(STORE).add(x));export const save=(db,x)=>req(db.transaction(STORE,'readwrite').objectStore(STORE).put(x));export const remove=(db,id)=>req(db.transaction(STORE,'readwrite').objectStore(STORE).delete(id));
