export function parseGPX(t,file){const x=new DOMParser().parseFromString(t,'application/xml');if(x.querySelector('parsererror'))throw Error('XML non valido');let es=[...x.querySelectorAll('trkpt')];if(!es.length)es=[...x.querySelectorAll('rtept')];const p=es.map(e=>({lat:+e.getAttribute('lat'),lon:+e.getAttribute('lon'),ele:e.querySelector('ele')?+e.querySelector('ele').textContent:null})).filter(a=>Number.isFinite(a.lat)&&Number.isFinite(a.lon));if(p.length<2)throw Error('Percorso non valido');let km=0,gain=0;for(let i=1;i<p.length;i++){km+=hav(p[i-1],p[i]);if(Number.isFinite(p[i-1].ele)&&Number.isFinite(p[i].ele)&&p[i].ele>p[i-1].ele)gain+=p[i].ele-p[i-1].ele}const n=(x.querySelector('trk>name')||x.querySelector('rte>name')||{}).textContent?.trim()||file.replace(/\.gpx$/i,'');return{name:n,fileName:file,points:p,distanceKm:km,elevationGain:Math.round(gain),zone:`${p[0].lat.toFixed(2)}, ${p[0].lon.toFixed(2)}`}}function hav(a,b){const R=6371,d=Math.PI/180,dl=(b.lat-a.lat)*d,dn=(b.lon-a.lon)*d,q=Math.sin(dl/2)**2+Math.cos(a.lat*d)*Math.cos(b.lat*d)*Math.sin(dn/2)**2;return R*2*Math.atan2(Math.sqrt(q),Math.sqrt(1-q))}
